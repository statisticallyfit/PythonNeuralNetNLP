name: paper_BSG_env_0
channels:
  - conda-forge
  - defaults
dependencies:

  # ========================
  # Core
  # ========================
  #- mypy
  #- typing
  - git # to be able to install the lasagne git package via git
  - pip
  ## python==2.7
  - jupytext
  - jupyterlab
  - ipywidgets
  - python-language-server
  - ipykernel 




  # ========================
  # NLP (important base nlp pckgs)
  # ========================
  #- pip:
  - theano #==0.9.0
  - numpy #==1.14.2
  - nltk #==3.2.2
  - scipy #==0.18.1
  #  - "--editable=git+https://github.com/Lasagne/Lasagne.git#egg=Lasagne-0.2.dev1"
  
prefix: /development/bin/python/miniconda3/envs/paper_BSG_env_0


### PLAN FOR CReATING THIS ENVIRONMENT ALREADY:
# 1. install as is here from fresh yml via conda
# 2. once installed, update the python version to 2.7
# 3. update theano, numpy, nltk, scipy to appropriate versions
# 4. while updating theano, simultaneously update lasagne as here: 
# ---> https://lasagne.readthedocs.io/en/latest/user/installation.html#bleeding-edge-version


### UPDATE: could not install python 2.7 because of conflicts. 
# Kept the python 3.9.6 installed by default

### ACTUAL STEPS: 
# conda env create -f paper_Brazinskas_BSG_env.yml 
# conda activate paper_BSG_env_0
# pip install --upgrade https://github.com/Theano/Theano/archive/master.zip
# pip install --upgrade https://github.com/Lasagne/Lasagne/archive/master.zip


### CONSEQUENCES: will now have to edit the BSG repo
# to fix print statements to have parentheses (and who knows what other py2 -> py3 changes?)
