name: pythinc_env
channels:
  - conda-forge
  - defaults
dependencies:

  - pytorch
  - pytorch-pretrained-bert
  - transformers
  - tensorflow>2.0

 
  - pip:
      - thinc>=8.0.0a0
      - ml_datasets
      - tqdm>=4.41
      - mxnet>=1.5.1,<1.6.0
      - gluonnlp
      #- tensorflow-gpu
prefix: /development/bin/python/conda3_ana/envs/pythinc_env

